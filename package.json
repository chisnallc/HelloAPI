{
  "name": "helloapi",
  "version": "1.0.0",
  "description": "Helloapi",
  "main": "index.js",
  "scripts": {
    "test": "npm test",
    "build": "rm -rf build && mkdir build && cp -R src/* build && cp package.json build && cp package-lock.json build && cd build && npm ci --production && cd .. && cd build && zip -9 -r package.zip . && cd .."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chisnallc/HelloAPI.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/chisnallc/HelloAPI/issues"
  },
  "homepage": "https://github.com/chisnallc/HelloAPI#readme"
}
